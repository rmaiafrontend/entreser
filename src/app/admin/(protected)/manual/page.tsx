import { ManualView } from '@/features/admin/manual/manual-view'

/** `/admin/manual` — manual da plataforma (como configurar tudo no backoffice). */
export default function ManualPage() {
  return <ManualView />
}
