import { OnboardingView } from '@/features/usuaria/onboarding/onboarding-view'

/** Onboarding inicial — inferência de fase (UF1). */
export default function OnboardingPage() {
  return <OnboardingView />
}
